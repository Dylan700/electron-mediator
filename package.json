{
  "name": "@dylan700/electron-mediator",
  "version": "1.0.0",
  "description": "Easily mediate requests between the main and renderer processes in electron.",
  "main": "renderer/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/Dylan700/electron-mediator.git"
  },
  "scripts": {
    "build": "tsc -p ./src",
    "clean": "rimraf renderer main",
    "test": "echo \"Error: no test specified\" && exit 1",
    "publish": "npm run build && npm publish"
  },
  "license": "ISC",
  "dependencies": {
    "electron": ">=12.0.0"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^4.9.4"
  },
  "files": [
    "renderer",
    "main"
  ]
}
